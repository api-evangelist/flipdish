{
  "opencollection": "1.0.0",
  "info": {
    "name": "Flipdish API - Apps Accounts ContentSecurityPolicy API",
    "version": "v1.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "https://api.flipdish.co/identity/connect/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "ContentSecurityPolicy",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Flipdish Content Security Policy_Report",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.flipdish.co/api/v1.0/csp/report",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "oauth2",
              "flow": "client_credentials",
              "accessTokenUrl": "https://api.flipdish.co/identity/connect/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Content Security Policy_Report via the Flipdish Open API v1.0 (POST /api/v1.0/csp/report)."
        }
      ]
    }
  ],
  "bundled": true
}