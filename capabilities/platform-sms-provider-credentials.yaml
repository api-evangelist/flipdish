naftiko: "1.0.0-alpha2"

info:
  label: "Flipdish API - Platform — SmsProviderCredentials"
  description: >-
    SmsProviderCredentials operations on the Flipdish Open API v1.0 (5 operations).
    Lead operation: Flipdish Get Brand Sms Provider Credentials. Self-contained Naftiko capability covering one Flipdish business surface.
  tags:
    - Flipdish
    - Online Ordering
    - Sms Provider Credentials
  created: "2026-06-02"
  modified: "2026-06-02"

binds:
  - namespace: env
    keys:
      FLIPDISH_TOKEN: FLIPDISH_TOKEN

capability:

  consumes:
    - type: http
      namespace: "platform-sms-provider-credentials"
      baseUri: "https://api.flipdish.co"
      description: "Flipdish API - Platform — SmsProviderCredentials business capability. Self-contained, no shared references."
      authentication:
        type: bearer
        token: "{{env.FLIPDISH_TOKEN}}"
      resources:
        - name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials"
          path: "/api/v1.0/sms-providers/orgs/{orgId}/brands/{brandId}/provider-credentials"
          operations:
            - name: "GetBrandSmsProviderCredentials"
              method: GET
              description: "Flipdish Get Brand Sms Provider Credentials"
              inputParameters:
                - name: "orgId"
                  in: path
                  type: string
                  required: true
                  description: "The org id path parameter."
                - name: "brandId"
                  in: path
                  type: string
                  required: true
                  description: "The brand id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          path: "/api/v1.0/sms-providers/orgs/{orgId}/brands/{brandId}/provider-credentials/{serviceType}"
          operations:
            - name: "GetBrandSmsProviderCredentialByServiceType"
              method: GET
              description: "Flipdish Get Brand Sms Provider Credential by Service Type"
              inputParameters:
                - name: "orgId"
                  in: path
                  type: string
                  required: true
                  description: "The org id path parameter."
                - name: "brandId"
                  in: path
                  type: string
                  required: true
                  description: "The brand id path parameter."
                - name: "serviceType"
                  in: path
                  type: string
                  required: true
                  description: "The service type path parameter."
                - name: "withFallback"
                  in: query
                  type: boolean
                  required: false
                  description: "The with fallback query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          path: "/api/v1.0/sms-providers/orgs/{orgId}/brands/{brandId}/provider-credentials/{serviceType}"
          operations:
            - name: "SmsProviderCredentialUpdate"
              method: POST
              description: "Flipdish Sms Provider Credential Update"
              inputParameters:
                - name: "orgId"
                  in: path
                  type: string
                  required: true
                  description: "The org id path parameter."
                - name: "brandId"
                  in: path
                  type: string
                  required: true
                  description: "The brand id path parameter."
                - name: "serviceType"
                  in: path
                  type: string
                  required: true
                  description: "The service type path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          path: "/api/v1.0/sms-providers/orgs/{orgId}/brands/{brandId}/provider-credentials/{serviceType}"
          operations:
            - name: "SmsProviderCredentialDelete"
              method: DELETE
              description: "Flipdish Sms Provider Credential Delete"
              inputParameters:
                - name: "orgId"
                  in: path
                  type: string
                  required: true
                  description: "The org id path parameter."
                - name: "brandId"
                  in: path
                  type: string
                  required: true
                  description: "The brand id path parameter."
                - name: "serviceType"
                  in: path
                  type: string
                  required: true
                  description: "The service type path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "sms-providers-bulk-upsert"
          path: "/api/v1.0/sms-providers/bulk-upsert"
          operations:
            - name: "SmsProviderCredentialsBulkUpsert"
              method: POST
              description: "Flipdish Sms Provider Credentials Bulk Upsert"
              inputParameters:
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."

  exposes:
    - type: rest
      namespace: "platform-sms-provider-credentials-rest"
      port: 8080
      description: "REST adapter for Flipdish API - Platform — SmsProviderCredentials."
      resources:
        - path: "/v1/sms-providers/orgs/{org-id}/brands/{brand-id}/provider-credentials"
          name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials"
          description: "REST surface for GetBrandSmsProviderCredentials."
          operations:
            - method: GET
              name: "GetBrandSmsProviderCredentials"
              description: "Flipdish Get Brand Sms Provider Credentials"
              call: "platform-sms-provider-credentials.GetBrandSmsProviderCredentials"
              with:
                "orgId": "rest.orgId"
                "brandId": "rest.brandId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/sms-providers/orgs/{org-id}/brands/{brand-id}/provider-credentials/{service-type}"
          name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          description: "REST surface for GetBrandSmsProviderCredentialByServiceType."
          operations:
            - method: GET
              name: "GetBrandSmsProviderCredentialByServiceType"
              description: "Flipdish Get Brand Sms Provider Credential by Service Type"
              call: "platform-sms-provider-credentials.GetBrandSmsProviderCredentialByServiceType"
              with:
                "orgId": "rest.orgId"
                "brandId": "rest.brandId"
                "serviceType": "rest.serviceType"
                "withFallback": "rest.withFallback"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/sms-providers/orgs/{org-id}/brands/{brand-id}/provider-credentials/{service-type}"
          name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          description: "REST surface for SmsProviderCredentialUpdate."
          operations:
            - method: POST
              name: "SmsProviderCredentialUpdate"
              description: "Flipdish Sms Provider Credential Update"
              call: "platform-sms-provider-credentials.SmsProviderCredentialUpdate"
              with:
                "orgId": "rest.orgId"
                "brandId": "rest.brandId"
                "serviceType": "rest.serviceType"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/sms-providers/orgs/{org-id}/brands/{brand-id}/provider-credentials/{service-type}"
          name: "sms-providers-orgs-org-id-brands-brand-id-provider-credentials-service-type"
          description: "REST surface for SmsProviderCredentialDelete."
          operations:
            - method: DELETE
              name: "SmsProviderCredentialDelete"
              description: "Flipdish Sms Provider Credential Delete"
              call: "platform-sms-provider-credentials.SmsProviderCredentialDelete"
              with:
                "orgId": "rest.orgId"
                "brandId": "rest.brandId"
                "serviceType": "rest.serviceType"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/sms-providers/bulk-upsert"
          name: "sms-providers-bulk-upsert"
          description: "REST surface for SmsProviderCredentialsBulkUpsert."
          operations:
            - method: POST
              name: "SmsProviderCredentialsBulkUpsert"
              description: "Flipdish Sms Provider Credentials Bulk Upsert"
              call: "platform-sms-provider-credentials.SmsProviderCredentialsBulkUpsert"
              with:
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."

    - type: mcp
      namespace: "platform-sms-provider-credentials-mcp"
      port: 9090
      transport: http
      description: "MCP adapter for Flipdish API - Platform — SmsProviderCredentials."
      tools:
        - name: "get-brand-sms-provider-credentials"
          description: "Flipdish Get Brand Sms Provider Credentials"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "platform-sms-provider-credentials.GetBrandSmsProviderCredentials"
          with:
            "orgId": "tools.orgId"
            "brandId": "tools.brandId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-brand-sms-provider-credential"
          description: "Flipdish Get Brand Sms Provider Credential by Service Type"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "platform-sms-provider-credentials.GetBrandSmsProviderCredentialByServiceType"
          with:
            "orgId": "tools.orgId"
            "brandId": "tools.brandId"
            "serviceType": "tools.serviceType"
            "withFallback": "tools.withFallback"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "sms-provider-credential-update"
          description: "Flipdish Sms Provider Credential Update"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "platform-sms-provider-credentials.SmsProviderCredentialUpdate"
          with:
            "orgId": "tools.orgId"
            "brandId": "tools.brandId"
            "serviceType": "tools.serviceType"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "sms-provider-credential-delete"
          description: "Flipdish Sms Provider Credential Delete"
          hints:
            readOnly: false
            destructive: true
            idempotent: true
          call: "platform-sms-provider-credentials.SmsProviderCredentialDelete"
          with:
            "orgId": "tools.orgId"
            "brandId": "tools.brandId"
            "serviceType": "tools.serviceType"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "sms-provider-credentials-bulk-upsert"
          description: "Flipdish Sms Provider Credentials Bulk Upsert"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "platform-sms-provider-credentials.SmsProviderCredentialsBulkUpsert"
          with:
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
