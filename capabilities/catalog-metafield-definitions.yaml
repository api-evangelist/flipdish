naftiko: "1.0.0-alpha2"

info:
  label: "Flipdish API - Catalog — MetafieldDefinitions"
  description: >-
    MetafieldDefinitions operations on the Flipdish Open API v1.0 (6 operations).
    Lead operation: Flipdish Get Owner Entity Configurations. Self-contained Naftiko capability covering one Flipdish business surface.
  tags:
    - Flipdish
    - Online Ordering
    - Metafield Definitions
  created: "2026-06-02"
  modified: "2026-06-02"

binds:
  - namespace: env
    keys:
      FLIPDISH_TOKEN: FLIPDISH_TOKEN

capability:

  consumes:
    - type: http
      namespace: "catalog-metafield-definitions"
      baseUri: "https://api.flipdish.co"
      description: "Flipdish API - Catalog — MetafieldDefinitions business capability. Self-contained, no shared references."
      authentication:
        type: bearer
        token: "{{env.FLIPDISH_TOKEN}}"
      resources:
        - name: "app-id-metafields-definitions"
          path: "/api/v1.0/{appId}/metafields/definitions"
          operations:
            - name: "GetOwnerEntityConfigurations"
              method: GET
              description: "Flipdish Get Owner Entity Configurations"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-metafields-definitions-owner-entity-key"
          path: "/api/v1.0/{appId}/metafields/definitions/{ownerEntity}/{key}"
          operations:
            - name: "GetMetafieldDefinitionByKey"
              method: GET
              description: "Flipdish Get Metafield Definition by Key"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "ownerEntity"
                  in: path
                  type: string
                  required: true
                  description: "The owner entity path parameter."
                - name: "key"
                  in: path
                  type: string
                  required: true
                  description: "The key path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-metafields-definitions-owner-entity-key"
          path: "/api/v1.0/{appId}/metafields/definitions/{ownerEntity}/{key}"
          operations:
            - name: "UpdateMetafieldDefinition"
              method: POST
              description: "Flipdish Update Metafield Definition"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "ownerEntity"
                  in: path
                  type: string
                  required: true
                  description: "The owner entity path parameter."
                - name: "key"
                  in: path
                  type: string
                  required: true
                  description: "The key path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-metafields-definitions-owner-entity"
          path: "/api/v1.0/{appId}/metafields/definitions/{ownerEntity}"
          operations:
            - name: "GetMetafieldDefinitions"
              method: GET
              description: "Flipdish Get Metafield Definitions"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "ownerEntity"
                  in: path
                  type: string
                  required: true
                  description: "The owner entity path parameter."
                - name: "searchTerm"
                  in: query
                  type: string
                  required: false
                  description: "The search term query parameter."
                - name: "page"
                  in: query
                  type: integer
                  required: false
                  description: "The page query parameter."
                - name: "limit"
                  in: query
                  type: integer
                  required: false
                  description: "The limit query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-metafields-definitions-owner-entity"
          path: "/api/v1.0/{appId}/metafields/definitions/{ownerEntity}"
          operations:
            - name: "CreateMetafieldDefinition"
              method: POST
              description: "Flipdish Create Metafield Definition"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "ownerEntity"
                  in: path
                  type: string
                  required: true
                  description: "The owner entity path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-metafields-definitions-recommendations-owner-entity"
          path: "/api/v1.0/{appId}/metafields/definitions/recommendations/{ownerEntity}"
          operations:
            - name: "GetOwnerEntityRecommendations"
              method: GET
              description: "Flipdish Get Owner Entity Recommendations"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "ownerEntity"
                  in: path
                  type: string
                  required: true
                  description: "The owner entity path parameter."
                - name: "searchTerm"
                  in: query
                  type: string
                  required: false
                  description: "The search term query parameter."
                - name: "page"
                  in: query
                  type: integer
                  required: false
                  description: "The page query parameter."
                - name: "limit"
                  in: query
                  type: integer
                  required: false
                  description: "The limit query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."

  exposes:
    - type: rest
      namespace: "catalog-metafield-definitions-rest"
      port: 8080
      description: "REST adapter for Flipdish API - Catalog — MetafieldDefinitions."
      resources:
        - path: "/v1/{app-id}/metafields/definitions"
          name: "app-id-metafields-definitions"
          description: "REST surface for GetOwnerEntityConfigurations."
          operations:
            - method: GET
              name: "GetOwnerEntityConfigurations"
              description: "Flipdish Get Owner Entity Configurations"
              call: "catalog-metafield-definitions.GetOwnerEntityConfigurations"
              with:
                "appId": "rest.appId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/metafields/definitions/{owner-entity}/{key}"
          name: "app-id-metafields-definitions-owner-entity-key"
          description: "REST surface for GetMetafieldDefinitionByKey."
          operations:
            - method: GET
              name: "GetMetafieldDefinitionByKey"
              description: "Flipdish Get Metafield Definition by Key"
              call: "catalog-metafield-definitions.GetMetafieldDefinitionByKey"
              with:
                "appId": "rest.appId"
                "ownerEntity": "rest.ownerEntity"
                "key": "rest.key"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/metafields/definitions/{owner-entity}/{key}"
          name: "app-id-metafields-definitions-owner-entity-key"
          description: "REST surface for UpdateMetafieldDefinition."
          operations:
            - method: POST
              name: "UpdateMetafieldDefinition"
              description: "Flipdish Update Metafield Definition"
              call: "catalog-metafield-definitions.UpdateMetafieldDefinition"
              with:
                "appId": "rest.appId"
                "ownerEntity": "rest.ownerEntity"
                "key": "rest.key"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/metafields/definitions/{owner-entity}"
          name: "app-id-metafields-definitions-owner-entity"
          description: "REST surface for GetMetafieldDefinitions."
          operations:
            - method: GET
              name: "GetMetafieldDefinitions"
              description: "Flipdish Get Metafield Definitions"
              call: "catalog-metafield-definitions.GetMetafieldDefinitions"
              with:
                "appId": "rest.appId"
                "ownerEntity": "rest.ownerEntity"
                "searchTerm": "rest.searchTerm"
                "page": "rest.page"
                "limit": "rest.limit"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/metafields/definitions/{owner-entity}"
          name: "app-id-metafields-definitions-owner-entity"
          description: "REST surface for CreateMetafieldDefinition."
          operations:
            - method: POST
              name: "CreateMetafieldDefinition"
              description: "Flipdish Create Metafield Definition"
              call: "catalog-metafield-definitions.CreateMetafieldDefinition"
              with:
                "appId": "rest.appId"
                "ownerEntity": "rest.ownerEntity"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/metafields/definitions/recommendations/{owner-entity}"
          name: "app-id-metafields-definitions-recommendations-owner-entity"
          description: "REST surface for GetOwnerEntityRecommendations."
          operations:
            - method: GET
              name: "GetOwnerEntityRecommendations"
              description: "Flipdish Get Owner Entity Recommendations"
              call: "catalog-metafield-definitions.GetOwnerEntityRecommendations"
              with:
                "appId": "rest.appId"
                "ownerEntity": "rest.ownerEntity"
                "searchTerm": "rest.searchTerm"
                "page": "rest.page"
                "limit": "rest.limit"
              outputParameters:
                - type: object
                  mapping: "$."

    - type: mcp
      namespace: "catalog-metafield-definitions-mcp"
      port: 9090
      transport: http
      description: "MCP adapter for Flipdish API - Catalog — MetafieldDefinitions."
      tools:
        - name: "get-owner-entity-configurations"
          description: "Flipdish Get Owner Entity Configurations"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-metafield-definitions.GetOwnerEntityConfigurations"
          with:
            "appId": "tools.appId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-metafield-definition-key"
          description: "Flipdish Get Metafield Definition by Key"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-metafield-definitions.GetMetafieldDefinitionByKey"
          with:
            "appId": "tools.appId"
            "ownerEntity": "tools.ownerEntity"
            "key": "tools.key"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "update-metafield-definition"
          description: "Flipdish Update Metafield Definition"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-metafield-definitions.UpdateMetafieldDefinition"
          with:
            "appId": "tools.appId"
            "ownerEntity": "tools.ownerEntity"
            "key": "tools.key"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-metafield-definitions"
          description: "Flipdish Get Metafield Definitions"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-metafield-definitions.GetMetafieldDefinitions"
          with:
            "appId": "tools.appId"
            "ownerEntity": "tools.ownerEntity"
            "searchTerm": "tools.searchTerm"
            "page": "tools.page"
            "limit": "tools.limit"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "create-metafield-definition"
          description: "Flipdish Create Metafield Definition"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-metafield-definitions.CreateMetafieldDefinition"
          with:
            "appId": "tools.appId"
            "ownerEntity": "tools.ownerEntity"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-owner-entity-recommendations"
          description: "Flipdish Get Owner Entity Recommendations"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-metafield-definitions.GetOwnerEntityRecommendations"
          with:
            "appId": "tools.appId"
            "ownerEntity": "tools.ownerEntity"
            "searchTerm": "tools.searchTerm"
            "page": "tools.page"
            "limit": "tools.limit"
          outputParameters:
            - type: object
              mapping: "$."
