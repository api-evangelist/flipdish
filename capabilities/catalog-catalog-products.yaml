naftiko: "1.0.0-alpha2"

info:
  label: "Flipdish API - Catalog — CatalogProducts"
  description: >-
    CatalogProducts operations on the Flipdish Open API v1.0 (6 operations).
    Lead operation: Flipdish Get Product by Id. Self-contained Naftiko capability covering one Flipdish business surface.
  tags:
    - Flipdish
    - Online Ordering
    - Catalog Products
  created: "2026-06-02"
  modified: "2026-06-02"

binds:
  - namespace: env
    keys:
      FLIPDISH_TOKEN: FLIPDISH_TOKEN

capability:

  consumes:
    - type: http
      namespace: "catalog-catalog-products"
      baseUri: "https://api.flipdish.co"
      description: "Flipdish API - Catalog — CatalogProducts business capability. Self-contained, no shared references."
      authentication:
        type: bearer
        token: "{{env.FLIPDISH_TOKEN}}"
      resources:
        - name: "app-id-catalog-products-catalog-item-id"
          path: "/api/v1.0/{appId}/catalog/products/{catalogItemId}"
          operations:
            - name: "GetProductById"
              method: GET
              description: "Flipdish Get Product by Id"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-products-catalog-item-id"
          path: "/api/v1.0/{appId}/catalog/products/{catalogItemId}"
          operations:
            - name: "UpdateProduct"
              method: POST
              description: "Flipdish Update Product"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-products"
          path: "/api/v1.0/{appId}/catalog/products"
          operations:
            - name: "GetProducts"
              method: GET
              description: "Flipdish Get Products"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "productTypes"
                  in: query
                  type: array
                  required: true
                  description: "The product types query parameter."
                - name: "searchTerm"
                  in: query
                  type: string
                  required: false
                  description: "The search term query parameter."
                - name: "page"
                  in: query
                  type: integer
                  required: false
                  description: "The page query parameter."
                - name: "limit"
                  in: query
                  type: integer
                  required: false
                  description: "The limit query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-products"
          path: "/api/v1.0/{appId}/catalog/products"
          operations:
            - name: "CreateProduct"
              method: POST
              description: "Flipdish Create Product"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-products-catalog-item-id-archive"
          path: "/api/v1.0/{appId}/catalog/products/{catalogItemId}/archive"
          operations:
            - name: "ArchiveProduct"
              method: POST
              description: "Flipdish Archive Product"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-products-catalog-item-id-duplicate"
          path: "/api/v1.0/{appId}/catalog/products/{catalogItemId}/duplicate"
          operations:
            - name: "DuplicateProduct"
              method: POST
              description: "Flipdish Duplicate Product"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."

  exposes:
    - type: rest
      namespace: "catalog-catalog-products-rest"
      port: 8080
      description: "REST adapter for Flipdish API - Catalog — CatalogProducts."
      resources:
        - path: "/v1/{app-id}/catalog/products/{catalog-item-id}"
          name: "app-id-catalog-products-catalog-item-id"
          description: "REST surface for GetProductById."
          operations:
            - method: GET
              name: "GetProductById"
              description: "Flipdish Get Product by Id"
              call: "catalog-catalog-products.GetProductById"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/products/{catalog-item-id}"
          name: "app-id-catalog-products-catalog-item-id"
          description: "REST surface for UpdateProduct."
          operations:
            - method: POST
              name: "UpdateProduct"
              description: "Flipdish Update Product"
              call: "catalog-catalog-products.UpdateProduct"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/products"
          name: "app-id-catalog-products"
          description: "REST surface for GetProducts."
          operations:
            - method: GET
              name: "GetProducts"
              description: "Flipdish Get Products"
              call: "catalog-catalog-products.GetProducts"
              with:
                "appId": "rest.appId"
                "productTypes": "rest.productTypes"
                "searchTerm": "rest.searchTerm"
                "page": "rest.page"
                "limit": "rest.limit"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/products"
          name: "app-id-catalog-products"
          description: "REST surface for CreateProduct."
          operations:
            - method: POST
              name: "CreateProduct"
              description: "Flipdish Create Product"
              call: "catalog-catalog-products.CreateProduct"
              with:
                "appId": "rest.appId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/products/{catalog-item-id}/archive"
          name: "app-id-catalog-products-catalog-item-id-archive"
          description: "REST surface for ArchiveProduct."
          operations:
            - method: POST
              name: "ArchiveProduct"
              description: "Flipdish Archive Product"
              call: "catalog-catalog-products.ArchiveProduct"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/products/{catalog-item-id}/duplicate"
          name: "app-id-catalog-products-catalog-item-id-duplicate"
          description: "REST surface for DuplicateProduct."
          operations:
            - method: POST
              name: "DuplicateProduct"
              description: "Flipdish Duplicate Product"
              call: "catalog-catalog-products.DuplicateProduct"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."

    - type: mcp
      namespace: "catalog-catalog-products-mcp"
      port: 9090
      transport: http
      description: "MCP adapter for Flipdish API - Catalog — CatalogProducts."
      tools:
        - name: "get-product-id"
          description: "Flipdish Get Product by Id"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-catalog-products.GetProductById"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "update-product"
          description: "Flipdish Update Product"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-products.UpdateProduct"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-products"
          description: "Flipdish Get Products"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-catalog-products.GetProducts"
          with:
            "appId": "tools.appId"
            "productTypes": "tools.productTypes"
            "searchTerm": "tools.searchTerm"
            "page": "tools.page"
            "limit": "tools.limit"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "create-product"
          description: "Flipdish Create Product"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-products.CreateProduct"
          with:
            "appId": "tools.appId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "archive-product"
          description: "Flipdish Archive Product"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-products.ArchiveProduct"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "duplicate-product"
          description: "Flipdish Duplicate Product"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-products.DuplicateProduct"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
