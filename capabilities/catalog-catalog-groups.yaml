naftiko: "1.0.0-alpha2"

info:
  label: "Flipdish API - Catalog — CatalogGroups"
  description: >-
    CatalogGroups operations on the Flipdish Open API v1.0 (6 operations).
    Lead operation: Flipdish Get Group by Id. Self-contained Naftiko capability covering one Flipdish business surface.
  tags:
    - Flipdish
    - Online Ordering
    - Catalog Groups
  created: "2026-06-02"
  modified: "2026-06-02"

binds:
  - namespace: env
    keys:
      FLIPDISH_TOKEN: FLIPDISH_TOKEN

capability:

  consumes:
    - type: http
      namespace: "catalog-catalog-groups"
      baseUri: "https://api.flipdish.co"
      description: "Flipdish API - Catalog — CatalogGroups business capability. Self-contained, no shared references."
      authentication:
        type: bearer
        token: "{{env.FLIPDISH_TOKEN}}"
      resources:
        - name: "app-id-catalog-groups-catalog-item-id"
          path: "/api/v1.0/{appId}/catalog/groups/{catalogItemId}"
          operations:
            - name: "GetGroupById"
              method: GET
              description: "Flipdish Get Group by Id"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-groups-catalog-item-id"
          path: "/api/v1.0/{appId}/catalog/groups/{catalogItemId}"
          operations:
            - name: "UpdateGroup"
              method: POST
              description: "Flipdish Update Group"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-groups"
          path: "/api/v1.0/{appId}/catalog/groups"
          operations:
            - name: "GetGroups"
              method: GET
              description: "Flipdish Get Groups"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "groupTypes"
                  in: query
                  type: array
                  required: true
                  description: "The group types query parameter."
                - name: "searchTerm"
                  in: query
                  type: string
                  required: false
                  description: "The search term query parameter."
                - name: "page"
                  in: query
                  type: integer
                  required: false
                  description: "The page query parameter."
                - name: "limit"
                  in: query
                  type: integer
                  required: false
                  description: "The limit query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-groups"
          path: "/api/v1.0/{appId}/catalog/groups"
          operations:
            - name: "CreateGroup"
              method: POST
              description: "Flipdish Create Group"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-groups-catalog-item-id-archive"
          path: "/api/v1.0/{appId}/catalog/groups/{catalogItemId}/archive"
          operations:
            - name: "ArchiveGroup"
              method: POST
              description: "Flipdish Archive Group"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-catalog-groups-catalog-item-id-duplicate"
          path: "/api/v1.0/{appId}/catalog/groups/{catalogItemId}/duplicate"
          operations:
            - name: "DuplicateGroup"
              method: POST
              description: "Flipdish Duplicate Group"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "catalogItemId"
                  in: path
                  type: string
                  required: true
                  description: "The catalog item id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."

  exposes:
    - type: rest
      namespace: "catalog-catalog-groups-rest"
      port: 8080
      description: "REST adapter for Flipdish API - Catalog — CatalogGroups."
      resources:
        - path: "/v1/{app-id}/catalog/groups/{catalog-item-id}"
          name: "app-id-catalog-groups-catalog-item-id"
          description: "REST surface for GetGroupById."
          operations:
            - method: GET
              name: "GetGroupById"
              description: "Flipdish Get Group by Id"
              call: "catalog-catalog-groups.GetGroupById"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/groups/{catalog-item-id}"
          name: "app-id-catalog-groups-catalog-item-id"
          description: "REST surface for UpdateGroup."
          operations:
            - method: POST
              name: "UpdateGroup"
              description: "Flipdish Update Group"
              call: "catalog-catalog-groups.UpdateGroup"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/groups"
          name: "app-id-catalog-groups"
          description: "REST surface for GetGroups."
          operations:
            - method: GET
              name: "GetGroups"
              description: "Flipdish Get Groups"
              call: "catalog-catalog-groups.GetGroups"
              with:
                "appId": "rest.appId"
                "groupTypes": "rest.groupTypes"
                "searchTerm": "rest.searchTerm"
                "page": "rest.page"
                "limit": "rest.limit"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/groups"
          name: "app-id-catalog-groups"
          description: "REST surface for CreateGroup."
          operations:
            - method: POST
              name: "CreateGroup"
              description: "Flipdish Create Group"
              call: "catalog-catalog-groups.CreateGroup"
              with:
                "appId": "rest.appId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/groups/{catalog-item-id}/archive"
          name: "app-id-catalog-groups-catalog-item-id-archive"
          description: "REST surface for ArchiveGroup."
          operations:
            - method: POST
              name: "ArchiveGroup"
              description: "Flipdish Archive Group"
              call: "catalog-catalog-groups.ArchiveGroup"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/catalog/groups/{catalog-item-id}/duplicate"
          name: "app-id-catalog-groups-catalog-item-id-duplicate"
          description: "REST surface for DuplicateGroup."
          operations:
            - method: POST
              name: "DuplicateGroup"
              description: "Flipdish Duplicate Group"
              call: "catalog-catalog-groups.DuplicateGroup"
              with:
                "appId": "rest.appId"
                "catalogItemId": "rest.catalogItemId"
              outputParameters:
                - type: object
                  mapping: "$."

    - type: mcp
      namespace: "catalog-catalog-groups-mcp"
      port: 9090
      transport: http
      description: "MCP adapter for Flipdish API - Catalog — CatalogGroups."
      tools:
        - name: "get-group-id"
          description: "Flipdish Get Group by Id"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-catalog-groups.GetGroupById"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "update-group"
          description: "Flipdish Update Group"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-groups.UpdateGroup"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "get-groups"
          description: "Flipdish Get Groups"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "catalog-catalog-groups.GetGroups"
          with:
            "appId": "tools.appId"
            "groupTypes": "tools.groupTypes"
            "searchTerm": "tools.searchTerm"
            "page": "tools.page"
            "limit": "tools.limit"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "create-group"
          description: "Flipdish Create Group"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-groups.CreateGroup"
          with:
            "appId": "tools.appId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "archive-group"
          description: "Flipdish Archive Group"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-groups.ArchiveGroup"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "duplicate-group"
          description: "Flipdish Duplicate Group"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "catalog-catalog-groups.DuplicateGroup"
          with:
            "appId": "tools.appId"
            "catalogItemId": "tools.catalogItemId"
          outputParameters:
            - type: object
              mapping: "$."
