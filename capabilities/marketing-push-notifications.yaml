naftiko: "1.0.0-alpha2"

info:
  label: "Flipdish API - Marketing — PushNotifications"
  description: >-
    PushNotifications operations on the Flipdish Open API v1.0 (4 operations).
    Lead operation: Flipdish Get Push Notifications. Self-contained Naftiko capability covering one Flipdish business surface.
  tags:
    - Flipdish
    - Online Ordering
    - Push Notifications
  created: "2026-06-02"
  modified: "2026-06-02"

binds:
  - namespace: env
    keys:
      FLIPDISH_TOKEN: FLIPDISH_TOKEN

capability:

  consumes:
    - type: http
      namespace: "marketing-push-notifications"
      baseUri: "https://api.flipdish.co"
      description: "Flipdish API - Marketing — PushNotifications business capability. Self-contained, no shared references."
      authentication:
        type: bearer
        token: "{{env.FLIPDISH_TOKEN}}"
      resources:
        - name: "app-id-pushnotifications"
          path: "/api/v1.0/{appId}/pushnotifications"
          operations:
            - name: "GetPushNotifications"
              method: GET
              description: "Flipdish Get Push Notifications"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "page"
                  in: query
                  type: integer
                  required: false
                  description: "The page query parameter."
                - name: "limit"
                  in: query
                  type: integer
                  required: false
                  description: "The limit query parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-pushnotifications"
          path: "/api/v1.0/{appId}/pushnotifications"
          operations:
            - name: "SchedulePushNotification"
              method: POST
              description: "Flipdish Schedule Push Notification"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-pushnotifications-scheduled-push-notification-id"
          path: "/api/v1.0/{appId}/pushnotifications/{scheduledPushNotificationId}"
          operations:
            - name: "UpdatePushNotification"
              method: POST
              description: "Flipdish Update Push Notification"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "scheduledPushNotificationId"
                  in: path
                  type: integer
                  required: true
                  description: "The scheduled push notification id path parameter."
                - name: body
                  in: body
                  type: object
                  required: true
                  description: "Request body payload."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
        - name: "app-id-pushnotifications-scheduled-push-notification-id"
          path: "/api/v1.0/{appId}/pushnotifications/{scheduledPushNotificationId}"
          operations:
            - name: "DeletePushNotification"
              method: DELETE
              description: "Flipdish Delete Push Notification"
              inputParameters:
                - name: "appId"
                  in: path
                  type: string
                  required: true
                  description: "The app id path parameter."
                - name: "scheduledPushNotificationId"
                  in: path
                  type: integer
                  required: true
                  description: "The scheduled push notification id path parameter."
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."

  exposes:
    - type: rest
      namespace: "marketing-push-notifications-rest"
      port: 8080
      description: "REST adapter for Flipdish API - Marketing — PushNotifications."
      resources:
        - path: "/v1/{app-id}/pushnotifications"
          name: "app-id-pushnotifications"
          description: "REST surface for GetPushNotifications."
          operations:
            - method: GET
              name: "GetPushNotifications"
              description: "Flipdish Get Push Notifications"
              call: "marketing-push-notifications.GetPushNotifications"
              with:
                "appId": "rest.appId"
                "page": "rest.page"
                "limit": "rest.limit"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/pushnotifications"
          name: "app-id-pushnotifications"
          description: "REST surface for SchedulePushNotification."
          operations:
            - method: POST
              name: "SchedulePushNotification"
              description: "Flipdish Schedule Push Notification"
              call: "marketing-push-notifications.SchedulePushNotification"
              with:
                "appId": "rest.appId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/pushnotifications/{scheduled-push-notification-id}"
          name: "app-id-pushnotifications-scheduled-push-notification-id"
          description: "REST surface for UpdatePushNotification."
          operations:
            - method: POST
              name: "UpdatePushNotification"
              description: "Flipdish Update Push Notification"
              call: "marketing-push-notifications.UpdatePushNotification"
              with:
                "appId": "rest.appId"
                "scheduledPushNotificationId": "rest.scheduledPushNotificationId"
                body: "rest.body"
              outputParameters:
                - type: object
                  mapping: "$."
        - path: "/v1/{app-id}/pushnotifications/{scheduled-push-notification-id}"
          name: "app-id-pushnotifications-scheduled-push-notification-id"
          description: "REST surface for DeletePushNotification."
          operations:
            - method: DELETE
              name: "DeletePushNotification"
              description: "Flipdish Delete Push Notification"
              call: "marketing-push-notifications.DeletePushNotification"
              with:
                "appId": "rest.appId"
                "scheduledPushNotificationId": "rest.scheduledPushNotificationId"
              outputParameters:
                - type: object
                  mapping: "$."

    - type: mcp
      namespace: "marketing-push-notifications-mcp"
      port: 9090
      transport: http
      description: "MCP adapter for Flipdish API - Marketing — PushNotifications."
      tools:
        - name: "get-push-notifications"
          description: "Flipdish Get Push Notifications"
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: "marketing-push-notifications.GetPushNotifications"
          with:
            "appId": "tools.appId"
            "page": "tools.page"
            "limit": "tools.limit"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "schedule-push-notification"
          description: "Flipdish Schedule Push Notification"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "marketing-push-notifications.SchedulePushNotification"
          with:
            "appId": "tools.appId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "update-push-notification"
          description: "Flipdish Update Push Notification"
          hints:
            readOnly: false
            destructive: false
            idempotent: false
          call: "marketing-push-notifications.UpdatePushNotification"
          with:
            "appId": "tools.appId"
            "scheduledPushNotificationId": "tools.scheduledPushNotificationId"
            body: "tools.body"
          outputParameters:
            - type: object
              mapping: "$."
        - name: "delete-push-notification"
          description: "Flipdish Delete Push Notification"
          hints:
            readOnly: false
            destructive: true
            idempotent: true
          call: "marketing-push-notifications.DeletePushNotification"
          with:
            "appId": "tools.appId"
            "scheduledPushNotificationId": "tools.scheduledPushNotificationId"
          outputParameters:
            - type: object
              mapping: "$."
